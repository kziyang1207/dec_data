# Offsite Test for Data Analyst Internship for KOH ZI YANG

To ensure smooth execution, run the following command and install the required dependencies

to set up the environment, please use python 3.10:
pip install -r requirements.txt


### Running the notebook
In this python notebook I have included a self-explore which include machine leaarning, If you wish to run the code, please set the variable run = True (default is False) as it will take longer time

Ensure the datasets are downloaded and present in the same directory as the notebook as well as replace df = pd.read_csv(your_own_directory)

